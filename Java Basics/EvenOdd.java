// The numbers we will be getting as input from the user.
// so, we need to use scanner class

import java.util.Scanner;

public class EvenOdd {
    System.out.println("Smallest element of array: "+ min);
    Scanner input = new Scanner(System.in);
    int n = input.nextInt(); // To take input as integer. nextline() or next() will take a string as input
    
}